baseurl: "" # the subpath of your site, e.g. /blog
# Use a real origin in dev so jekyll-seo-tag emits absolute og:url / JSON-LD @id (empty url breaks previews and some tools).
url: "http://localhost:4000"
